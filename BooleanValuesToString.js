// Complete the method that takes a boolean value and return a "Yes" string for true,
// or a "No" string for false.
const booleanToStrings = (a) => {
	if (a === true) {
		return "Yes";
	} else {
		return "No";
	}
};
console.log(booleanToStrings(true));
